import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gas.calc.luke.dev',
  appName: 'gas-calc',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
